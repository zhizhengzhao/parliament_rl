{
  "_description": "Parameters for reconstructing RL training context from parliament.db. All wrapping templates (intros, section headers, post/comment/vote formats, [you] marker, anonymous voter labels) live as a multi-variant pool in rl/extract.py:TEMPLATE_POOL. See that module's docstring for sampling logic.",

  "min_content_chars": 20,

  "anonymize_identity": true,

  "advantage_baseline": "mean_session",
  "advantage_scale": "session_std"
}
