# Parliament RL — pip dependencies.
#
# This file is kept for legacy workflows (`pip install -r requirements.txt`).
# The single source of truth for project metadata + dependencies is
# `pyproject.toml`; prefer:
#
#     pip install -e .              # core
#     pip install -e ".[fast]"      # + fused attention kernels
#     pip install -e ".[dev]"       # + pytest
#
# After install, every CLI is on $PATH (prl-server, prl-iterate, …).
# See `pyproject.toml:[project.scripts]` for the full list.
#
# Lower bounds reflect what's actually been validated on the A100 dev box.
# Pinned "known-good" versions are recorded in pyproject.toml comments —
# use those if you need bit-identical behaviour for reproducing results.

# Parliament server + harness (data collection)
fastapi>=0.135
uvicorn>=0.41
aiohttp>=3.13
pydantic>=2.0

# RL training (rl/train.py outputs PEFT LoRA adapter; rl/export.py
# merges adapter into base for the next iter's vLLM to load directly.)
torch>=2.10
transformers>=5.5
accelerate>=1.13
peft>=0.19
safetensors>=0.7

# Inference (vLLM HTTP fleet, DP=8 + TP=1, kill+merge+reload between
# iters — DP > TP for ≤13B models per OpenRLHF / verl docs.)
vllm>=0.19

# Optional fused kernels (~2-3x vLLM speedup on Qwen3.5 hybrid attention).
# Comment these out if your platform lacks pre-built wheels.
flash-linear-attention>=0.4
causal-conv1d>=1.6
flash-attn>=2.7

# Implicit but worth noting (pulled in by torch / transformers):
#   triton 3.6.0
#   numpy 2.2.6
