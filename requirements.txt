# Parliament server + harness (data collection)
fastapi>=0.100
uvicorn>=0.20
aiohttp>=3.9

# RL training (rl/train.py + rl/export.py)
torch>=2.5
transformers>=4.51
accelerate>=1.3
peft>=0.14
safetensors>=0.4

# Inference (vLLM serving for data generation)
vllm>=0.17

# Optional: fused kernels for Qwen3.5 hybrid attention (2-3x vLLM speedup)
# Pure Triton — no compilation needed, pip install and vLLM auto-detects.
flash-linear-attention>=0.4
causal-conv1d>=1.5
flash-attn>=2.7
